# ==========================================================
# Pawdoro Backend - "dev" profile
# Activate with: --spring.profiles.active=dev
# Intended for local development against the docker-compose
# PostgreSQL instance defined at the project root.
# ==========================================================

logging.level.root=INFO
logging.level.com.pawdoro.backend=DEBUG
logging.level.org.hibernate.SQL=DEBUG

spring.jpa.show-sql=true
